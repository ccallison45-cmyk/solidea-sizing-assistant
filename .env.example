# solidea-sizing-assistant Environment Variables
# Copy this file to .env and fill in real values.
# NEVER commit .env to version control.

# --- Application ---
APP_ENV=development
APP_PORT=8000
APP_LOG_LEVEL=info

# --- CORS ---
# Comma-separated list of allowed origins
ALLOWED_ORIGINS=https://solideaus.com,https://www.solideaus.com

# --- Sizing Data ---
# Path to the sizing data directory (relative to project root)
SIZING_DATA_DIR=data

# --- n8n Integration (only needed if running email automation) ---
# N8N_WEBHOOK_URL=REPLACE_ME

# --- Deployment (only needed for production deploys) ---
# RAILWAY_TOKEN=REPLACE_ME
# RENDER_API_KEY=REPLACE_ME
